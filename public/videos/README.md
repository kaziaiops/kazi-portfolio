Real footage lives here using these exact filenames — the site references them directly:

- `hero-reel.mp4` — full-bleed looping hero background montage
- `project-ceo-drama.mp4` — The CEO's Forbidden Assistant
- `project-ugc-ads.mp4` — UGC Talking-Head Ad Campaigns
- `project-spec-ads.mp4` — Spec Ad Portfolio

If a file here ever goes missing, the site falls back gracefully to its
matching poster image in `/public/images/` (or a plain gradient for the
hero) — nothing will look broken.

Keep these compressed for web (H.264 mp4) so the site stays fast.
